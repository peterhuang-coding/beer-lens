# Beer Lens 四小时算法迭代执行计划

**授权**：用户于2026-09-19要求自主定义算法迭代任务，使用Coding Plan连续工作4小时并提高并发。本计划落实已有购买决策方向，不开新产品方向。

**窗口**：北京时间2026-09-19 23:36:41—2026-09-20 03:36:41。最后10分钟停止新增派发；截止后只收尾，不补跑。全部通过可提前结束，不为空耗凑满4小时。

**架构**：Coding Plan承担公开代码和合成材料的分析、最小补丁、回归草稿。一个协调者按实测证据串行应用并验收；最多4个独立请求并发，无并发文件写入。沿用PR5分支，不自动合并main。当前3000服务不因研究自动部署。

## 线路与验收

| 线 | 文件边界 | 最小验收 | 后续迭代 |
|---|---|---|---|
| A 购买约束 | recommendation/purchase.ts及专用测试 | 中文菜单编号1—99、跳号和乱序保持正确；不存在/重复编号澄清；价量数字不能当编号；不突破预算 | 全称/别名比较的歧义、否定偏好；只修有反例的行为 |
| B 实体匹配 | .beer-data/lookup.py及合成SQLite测试 | 同名异厂、年份/版本、局部词重叠不误附评分；已核验标记保留；既有精确匹配不退化 | 置信度拒答与留出集，原库只读 |
| C 反馈归因 | taste-feedback/execute.ts及纯函数/合成测试 | 推荐不等于已饮用；明确名称或印刷编号唯一匹配；模糊反馈只追问不写记忆；否定不升级为喜好 | taste与would-buy-again区分，隔离目录验证真实状态变化 |
| D 延迟与评测 | lib/multimodal/cache.ts/container.ts及离线假provider测试 | 缓存键隔离prompt/schema/model；有界超时/并发；不跨用户泄露偏好；先验证再改 | 冷/暖请求分开计时，失败/命中不当新采样，回归报告保留前后输出 |

## 每包执行

- [ ] 恢复本轮state和已有attempt结果，确认未重复提交；公共代码片段不超过40000字符。
- [ ] 下游输出最小补丁和可复现合成反例，不接受“已执行”的无工具声明。
- [ ] 验证反例原实现确实失败；只应用必要补丁；回归和留出用例不能同步迎合新实现。
- [ ] 测试通过才保留，记review.json：采用/拒绝理由、请求/返回模型、tokens、耗时、测试证据。
- [ ] 合并前检查跨线接口；串行提交。每线至多一次质量修正；未知提交不重发。
- [ ] 里程碑跑必要的相关回归；最终在隔离构建目录做全量检查，避免覆盖正在服务的.next。
- [ ] 已验证改动推送现有分支、更新PR5，原始结果留本地；新图片评测不能覆盖旧报告。

## 配额和调度

火山volcengine-plan，默认doubao-seed-evolving；有证据需要修正时可在套餐内使用已验证ark-code-latest。无额外按量兜底。最多4并发，限流降2/1并等待，不循环轰炸。窗口总计最多24个下游请求（含修正）、24次真实API请求、10条真实爬虫记录；并非必须用满。每个下游请求超时至多180秒。

本线程heartbeat每10分钟恢复协调；首批4包已开始执行。调度依赖电脑和Codex可运行，不保证离线期间连续执行。到期/全部完成/用户停止时暂停；连续3轮同因无进展停该线，仍可做其他已授权线。

## 状态与交付

本地恢复入口：Hub项目test-runs/2026-09-19-four-hour/state.json，各线request/attempt/review。原有修改的数据库、selector基线、HTML及未跟踪文件全部保留，不提交。不得外发真实用户记忆、原始数据库、凭据；不得自动部署、合main、新增服务或无界采集。

最终交付：逐线采纳改动、前后测试/留出结果、运行耗时与实际套餐使用、GitHub提交、未通过项和一个下一步。真实消费者主观验证仍待用户；不能把合成回归通过当作产品准确率。
